"use strict";
//This line treats all js code as new version

//alert('any message)
//not run in console, it runs on browser

//There are 7 primitive datatype in javascript

let lang = "javascript"
//String " "

let ver = 20.2
//Number (Any integer, decimal, complex not alowed)

let status1 = true
//boolean (true/false) 

let n = null
//standalone value, it is value which have null value

let d;
//when we don't declare the value, it is undefined

console.table([lang, status1, n. d]);   